a simple roleplay game character creator, on windows you will need to install curses windows in order to make the program work
curses is a linux lib, to install it type pip install windows-curses in your windows terminal bye
